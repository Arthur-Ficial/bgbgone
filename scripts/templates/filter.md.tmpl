# `{{NAME}}`

> {{DOC}}

| Field | Value |
|---|---|
| **Layers** | {{LAYERS}} |
| **Signature** | `{{SIGNATURE}}` |
{{ALPHA_NOTE}}

## Example — `{{EXAMPLE_CHAIN}}`

The same operation through both transports. `scripts/gen-docs.sh` runs BOTH commands on every regen and asserts byte-identical output (parity contract). The result is shown below.

### Via CLI

```bash
{{EXAMPLE_INVOCATION}}
```

### Via HTTP server (`bgbgone --server`)

```bash
{{EXAMPLE_SERVER}}
```

![red-panda after `{{EXAMPLE_CHAIN}}` — CLI render = server render (byte-identical)](../images/filters/{{NAME}}.jpg)

{{YOGA_SECTION}}{{WOMAN_SINGER_SECTION}}

See the [filter index](README.md) for the full catalogue. Server-mode README: [`../../SERVER-README.md`](../../SERVER-README.md).
